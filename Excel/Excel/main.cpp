#include <stdio.h>
#include "excel.h"


int main() {
	createSheetAndExecute();
	/*int command_length = 0;
	char **command = tokenize("   ,   a,b,c,d,e,f,g  ,  g  g  100", &command_length);
	for (int i = 0; i < command_length; i++) {
		printf("%s\n", command[i]);
	}*/
	getchar();
}